# core
deepagents
langchain[openai]
langchain[anthropic]

# deployment and tracing
langsmith

# sandbox
langchain-daytona
# Notebook
ipykernel
jupyter

# Dev
python-dotenv
pytest
pytest-asyncio # registers the asyncio mark (so pytest stops warning) + actually awaits your async def tests

# ArXiv fetching
arxiv>=2.0.0
requests

# PDF processing
pypdf>=4.0.0
pymupdf